function ShirtDetails(color, size, print) {
    if (print === void 0) { print = "shark"; }
    console.log("The color of the T-shirt is ".concat(color, " with ").concat(size, " size and ").concat(print, " print."));
}
;
ShirtDetails("black", 25);
