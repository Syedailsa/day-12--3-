function ShirtDetails(color: string, size: number, print: string = "shark" ){
    console.log(`The color of the T-shirt is ${color} with ${size} size and ${print} print.`)
};
ShirtDetails("black", 25);
                        // shark becomes the default value incase if no value is provided
